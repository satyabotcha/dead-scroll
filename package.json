{
  "name": "dead-scroll",
  "version": "0.1.1",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "rm -rf production && mkdir -p production/popup production/assets && cp manifest.json production/manifest.json && cp src/popup/popup.html src/popup/popup.css production/popup/ && if [ -d src/assets ]; then cp -R src/assets/. production/assets/; fi && tsc && mkdir -p production/content && cp src/content/*.css production/content/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
